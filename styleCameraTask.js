import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {},
  iconContainer: {
    // margin: 20,
    flexDirection: 'row',
    justifyContent: 'space-evenly',
  },
  img: {
    // width: 40,
  },
});
